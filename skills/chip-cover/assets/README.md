# Assets intentionally not bundled

This Powerpack package does not include private Human20/HLRU ready-made cover backgrounds, channel logos, generated samples, or private brand assets.

Provide brand assets at runtime through brand-specific env vars / spec paths, or generate no-text backgrounds with the `img` skill and compose exact text/logo overlays deterministically.
