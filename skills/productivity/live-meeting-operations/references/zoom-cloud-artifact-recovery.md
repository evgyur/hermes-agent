# Zoom cloud artifact recovery — compatibility pointer

This filename is retained for deployed Human20Bot guards and older callers.

The only canonical implementation is:

- [`zoom-cloud-artifacts.md`](zoom-cloud-artifacts.md)
- `../scripts/zoom_cloud_artifacts.py`

Do not duplicate OAuth, selection, download, redaction, synthesis, or completion logic here. Follow the canonical reference and helper end to end.
