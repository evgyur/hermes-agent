---
name: human20-ops
description: Admin-only narrow Human20 operations via the governed broker.
---
# Human20 operations

Use only for an authenticated admin. Read the exact target before proposing a mutation. Raw shell, container, privilege, terminal, and helper bypasses are forbidden. Beta actions require focused tests and readback. Production actions require ledger-verified exact scope, then post-action readback. Without approval, return the stable production blocker.
